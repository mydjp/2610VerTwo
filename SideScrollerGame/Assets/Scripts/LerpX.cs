using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LerpX : MonoBehaviour
{
    
    //public GameObject Player;
    //public GameObject Cammera;
    //public float XPosition;
    //public float TTime;


    void Start()
    {
       // XPosition = transform.position.X;
    }

    // Update is called once per frame
    void Update()
    {
      //  XPosition = Mathf.Lerp(XPosition, Player.transform.position.x, TTime * Time.deltaTime);
        //Cammera.transform.position = new Vector3(XPosition, Cammera.transform.position.y, Cammera.transform.position.z);
    }
    
}
